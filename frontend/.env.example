# =============================================================================
# CatalogIQ Frontend — Environment Configuration
# =============================================================================
# For local development with Vite dev proxy (defaults to localhost:8000 via proxy):
# VITE_API_URL=
#
# For production deployment on Vercel:
VITE_API_URL=https://catalogiq-backend.onrender.com
